{
  "name": "formancy",
  "version": "0.0.0",
  "private": true,
  "description": "A modern, self-hostable form engine, builder and backend for React and Angular.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "check:pkg": "turbo run check:pkg"
  },
  "devDependencies": {
    "turbo": "^2.11.2",
    "typescript": "catalog:"
  }
}
