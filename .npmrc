# Strict isolation: no phantom dependencies. Load-bearing when publishing many
# packages with different peer ranges.
node-linker=isolated
strict-peer-dependencies=true
auto-install-peers=false
provenance=true
